---
tags:
  - sources
  - audit
updated: 2026-08-13
---

# 资料来源与扫描范围

## 已阅读/分析

- 根说明与配置：`README.md`、`CONCEPTS.md`、`package.json`、`tsconfig.json`、`vite.config.ts`、`index.html`、`metadata.json`。
- 忽略与环境示例：`.gitignore`、`gitignore`、`.env.example`、`env.example`、`.npmrc`。
- 后端：完整 `server.ts` 路由与辅助逻辑。
- 前端：`src/` 下全部 13 个源码文件的结构与核心业务逻辑。
- 脚本：`scripts/` 下全部三个脚本。
- 历史资料：`LAYOUT_ANALYSIS.md`、`QR_SIZE_DEBUG.md`、旧架构审计。
- Agent 资产：根据 `skills-lock.json` 与目录结构确认来源与职责；其第三方示例/参考文件按供应商资产统计，不逐段纳入业务架构。
- 生成物/缓存/实验目录：按文件类型、数量、体积和最大文件统计，不把二进制内容逐个读取。
- Git：工作树状态、已跟踪候选、历史大对象概况。

## 有意不复制

- `.env` 的值。
- 明文凭证文件的值。
- 二进制图片、ONNX 模型、构建产物和依赖包内容。
- 用户现有未提交改动的完整 diff（审计只避免覆盖）。

## 验证命令结果

- TypeScript：通过。
- Production build：通过。
- 未运行数据库/COS 集成测试，因为这会依赖真实外部凭证与数据。
- 未运行浏览器交互测试；当前没有现成 E2E 套件。

## 时间点

本知识库反映 2026-08-13 工作区状态。后续代码变化应同步更新各笔记的 `updated` 字段。
